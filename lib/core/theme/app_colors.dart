import 'package:flutter/material.dart';

class AppColors {
  // Primary — Lime
  static const lime500 = Color(0xFFC6FF3D);
  static const lime300 = Color(0xFFE3FFA0);
  static const lime700 = Color(0xFF8FCC00);

  // Secondary — Carbon
  static const carbon900 = Color(0xFF1C1F1E);
  static const carbon700 = Color(0xFF333937);
  static const muted     = Color(0xFF6B7268);

  // Neutrals
  static const white     = Color(0xFFFFFFFF);
  static const cream     = white; // Alias for white to maintain background consistency
  static const divider   = Color(0xFFE4E8DC);

  // Status & Accents
  static const gold      = Color(0xFFFFD700);
  static const onGold    = Color(0xFF000000);
}
